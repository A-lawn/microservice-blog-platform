server:
  port: 8082

spring:
  application:
    name: article-service
  
  profiles:
    active: dev
  
  cloud:
    nacos:
      server-addr: ${NACOS_SERVER_ADDR:localhost:8848}
      discovery:
        namespace: ${NACOS_NAMESPACE:dev}
        group: ${NACOS_GROUP:blog-platform}
        enabled: ${NACOS_DISCOVERY_ENABLED:true}
      config:
        import-check:
          enabled: false
  
  config:
    import:
      - optional:file:../../config/common-nacos.yml
      - optional:file:../../config/common-monitoring.yml
      - optional:file:../../config/common-seata.yml
      - optional:file:../../config/common-rocketmq.yml
      - optional:file:../../config/common-redis.yml
  
  datasource:
    article:
      jdbc-url: ${ARTICLE_DB_URL:jdbc:mysql://localhost:3306/article_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
      username: ${ARTICLE_DB_USERNAME:root}
      password: ${ARTICLE_DB_PASSWORD:root123}
      driver-class-name: com.mysql.cj.jdbc.Driver
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true

  
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
  
  data:
    elasticsearch:
      repositories:
        enabled: false
      uris: ${ELASTICSEARCH_URIS:http://localhost:9200}

  elasticsearch:
    index:
      auto-create: false

feature:
  cqrs:
    enabled: ${CQRS_ENABLED:false}

config:
  fallback:
    enabled: true
    cache-dir: ./config-cache
    cache-interval: 30
  retry:
    max-attempts: 3
    initial-delay: 1000
    max-delay: 10000
    multiplier: 2.0
    timeout: 30000

article-service:
  version: 1.0.0
  maxArticleLength: 50000
  defaultPageSize: 10
  enableDraft: true
  enableSearch: false
  searchResultLimit: 100
  metrics:
    export:
      prometheus:
        enabled: true

rocketmq:
  name-server: ${ROCKETMQ_NAME_SERVER:localhost:9876}
  producer:
    group: article-service-producer
    send-message-timeout: 3000
    retry-times-when-send-failed: 2

seata:
  enabled: false
  enable-auto-data-source-proxy: false

---
spring:
  config:
    activate:
      on-profile: docker
  
  datasource:
    article:
      jdbc-url: ${ARTICLE_DB_URL:jdbc:mysql://mysql:3306/article_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC}
  
  data:
    elasticsearch:
      uris: ${ELASTICSEARCH_URIS:http://elasticsearch:9200}
